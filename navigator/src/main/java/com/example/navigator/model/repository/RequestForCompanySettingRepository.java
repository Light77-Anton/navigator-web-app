package com.example.navigator.model.repository;
import com.example.navigator.model.RequestForCompanySetting;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestForCompanySettingRepository extends JpaRepository<RequestForCompanySetting, Long> {
}
